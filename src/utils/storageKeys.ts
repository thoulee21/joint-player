export enum StorageKeys {
    // eslint-disable-next-line no-unused-vars
    Keyword = 'keyword',
    // eslint-disable-next-line no-unused-vars
    BlurRadius = 'blurRadius',
    // eslint-disable-next-line no-unused-vars
    Favs = 'favs',
    // eslint-disable-next-line no-unused-vars
    User = 'user',
}
